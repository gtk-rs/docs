initSidebarItems({"struct":[["ApplicationWindow","`ApplicationWindow` is a `Window` subclass that offers some extra functionality for better integration with `Application` features. Notably, it can handle both the application menu as well as the menubar. See `Application::set_app_menu` and `Application::set_menubar`."]]});